# Keep Spark on YARN submissions pointing at the Router-backed Hadoop client config.
spark.master                     yarn
spark.submit.deployMode          cluster
spark.yarn.queue                 root.batch
spark.eventLog.enabled           true
spark.eventLog.dir               hdfs:///spark-history
spark.history.fs.logDirectory    hdfs:///spark-history
spark.yarn.submit.waitAppCompletion true

# Example security defaults for long-running jobs.
# spark.kerberos.principal       sparkuser@EXAMPLE.COM
# spark.kerberos.keytab          /etc/security/keytabs/sparkuser.keytab
